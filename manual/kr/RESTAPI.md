# RESTAPI

**종류**: EGTools 전용 함수

REST API를 호출해 응답 텍스트를 반환합니다.

## 구문

```
=RESTAPI(주소, [방법], [요청헤더], [요청본문])
```

## 인수

| 인수 | 필수 | 설명 |
|---|---|---|
| 주소 | 필수 | API URL(GET 파라미터 포함) |
| 방법 | 선택 | "GET"(기본), "POST", "PUT", "PATCH", "DELETE" 등 |
| 요청헤더 | 선택 | 헤더 이름/값 2열 범위 |
| 요청본문 | 선택 | 요청 본문 텍스트 |

## 반환

<!-- TODO: 반환 형태(스칼라/배열·스필)와 의미, 오류 조건(#VALUE!, #N/A 등) -->

## 예제

| 수식 | 결과 | 설명 |
|---|---|---|
| `=RESTAPI(...)` | | <!-- TODO --> |

## 참고

<!-- TODO: 원본(Excel/Google)과의 차이, 관련 함수 링크 -->
- 지원: Excel 2010+. 모든 Excel 버전에서 `RESTAPI` 이름 그대로 등록됩니다(`EG.`/`x` 접두어 없음).
