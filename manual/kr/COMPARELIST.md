# COMPARELIST

**종류**: EGTools 전용 함수

목록들을 비교해 지정한 집합을 출력합니다: 0 합집합, N번 목록 전용, -1 단독, -2 교집합, -3 부분교집합.

## 구문

```
=COMPARELIST(집합종류, 행단위, 배열1, ...)
```

## 인수

| 인수 | 필수 | 설명 |
|---|---|---|
| 집합종류 | 필수 | 0 합집합, N=N번 목록에만, -1 하나에만, -2 모두에, -3 일부에만 |
| 행단위 | 필수 | TRUE면 행 전체 비교, FALSE면 셀별 비교 |
| 배열1 | 필수 | 목록 범위 또는 배열 |
| … | 선택 | 위 인수(들)를 반복 지정할 수 있습니다 |

## 반환

<!-- TODO: 반환 형태(스칼라/배열·스필)와 의미, 오류 조건(#VALUE!, #N/A 등) -->

## 예제

| 수식 | 결과 | 설명 |
|---|---|---|
| `=COMPARELIST(...)` | | <!-- TODO --> |

## 참고

<!-- TODO: 원본(Excel/Google)과의 차이, 관련 함수 링크 -->
- 지원: Excel 2010+. 모든 Excel 버전에서 `COMPARELIST` 이름 그대로 등록됩니다(`EG.`/`x` 접두어 없음).
