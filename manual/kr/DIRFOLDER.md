# DIRFOLDER

**종류**: EGTools 전용 함수

지정 폴더의 파일(선택 시 폴더 포함) 목록을 출력합니다.

## 구문

```
=DIRFOLDER(경로, [필터], [폴더포함], [하위폴더], [숨김포함])
```

## 인수

| 인수 | 필수 | 설명 |
|---|---|---|
| 경로 | 필수 | 폴더 경로 |
| 필터 | 선택 | 파일 필터, 예 "*.xlsx;*.csv" (기본 *.*) |
| 폴더포함 | 선택 | TRUE면 폴더 이름 포함 |
| 하위폴더 | 선택 | TRUE면 하위 폴더까지 나열 |
| 숨김포함 | 선택 | TRUE면 숨김 항목 포함 |

## 반환

<!-- TODO: 반환 형태(스칼라/배열·스필)와 의미, 오류 조건(#VALUE!, #N/A 등) -->

## 예제

| 수식 | 결과 | 설명 |
|---|---|---|
| `=DIRFOLDER(...)` | | <!-- TODO --> |

## 참고

<!-- TODO: 원본(Excel/Google)과의 차이, 관련 함수 링크 -->
- 지원: Excel 2010+. 모든 Excel 버전에서 `DIRFOLDER` 이름 그대로 등록됩니다(`EG.`/`x` 접두어 없음).
